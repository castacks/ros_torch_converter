- name: racepak_pedal_pos
  group: sensors
  topic: /racepak/pedal_pos
  type: PedalPos
  max_age: 1.
  optional: true
  args:
    interpolation:
      type: MATCH_TOPIC
      topic: super_odometry/odometry

- name: racepak_shock_pos
  group: sensors
  topic: /racepak/shock_pos
  type: ShockPos
  max_age: 1.
  optional: true
  args:
    interpolation:
      type: MATCH_TOPIC
      topic: super_odometry/odometry

- name: racepak_wheel_rpm
  group: sensors
  topic: /racepak/wheel_rpm
  type: WheelRPM
  max_age: 1.
  optional: true
  args:
    interpolation:
      type: MATCH_TOPIC
      topic: super_odometry/odometry
